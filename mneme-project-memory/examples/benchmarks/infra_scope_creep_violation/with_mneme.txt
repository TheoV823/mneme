Enterprise readiness in v1 means making the core workflow reliable and easy
to integrate (rule-004). Complex distributed infrastructure is out of scope —
every v1 capability must run on a fresh installation with no accounts required.

The right approach: improve error handling and API versioning, strengthen the
CLI for team workflows, and document CI integration patterns. Prove value
with the core approach before adding infrastructure complexity.
