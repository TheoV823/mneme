Coordination across AI instances is out of scope for v1 (anti-002).
The v1 scope covers direct question-answering with project decision context —
not orchestrating interactions across AI systems.

For AI instances that need to respect project decisions, inject the context
packet into each system prompt individually. Coordination logic belongs at
a higher architectural level.
